{
  "name": "run-together",
  "version": "0.1.0",
  "private": true,
  "description": "Bilingual realtime PWA for runners to vote on shared practice days",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/server.js && node --check tests/app.test.js",
    "lint": "npm run check"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
